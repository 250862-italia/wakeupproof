services:
  - type: web
    name: wakeupproof-payments
    env: node
    buildCommand: echo "No build needed"
    startCommand: node deploy-server.js
    envVars:
      - key: NODE_ENV
        value: production
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed"
    build:
      commands:
        - echo "No build needed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed - static files"
    build:
      commands:
        - echo "Static files ready"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

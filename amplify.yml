version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build commands needed"
    build:
      commands:
        - echo "No build commands needed"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*